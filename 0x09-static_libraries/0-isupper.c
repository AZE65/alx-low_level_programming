#include "main.h"
/**
 * _isupper - Write a function that checks for uppercase character.
 * @c: parameter for function.
 * Return: is zero.
 */

int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
return (1);

return (0);
}
